// The "Edit this page" link now lives in the navbar (see
// src/theme/NavbarItem/EditThisPageNavbarItem), so the default footer
// link is suppressed here rather than shown twice.
export default function EditThisPage() {
  return null;
}
